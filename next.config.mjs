/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','replicate.delivery','firebasestorage.googleapis.com'],
      },
};
 
export default nextConfig;
