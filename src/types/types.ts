export interface Project {
    id: string;
    newImage: string;
    oldImage: string;
    requirements: string;
    roomType: string;
    selectedStyle: string;
    createdAt: string;
  }

  export interface UserData {
    id: string;
    name: string;
    email: string;
    image: string;
    credits: number;
    createdAt: string;
  }
  